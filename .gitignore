escrow.toml
escrow-cache/
/escrow
/escrow-cli
/escrow-darwin-*
/escrow-linux-*
/escrow-windows-*
/escrow-cli-darwin-*
/escrow-cli-linux-*
/escrow-cli-windows-*
.superpowers/

# Go unit-test coverage output (make test-unit)
coverage.out

# Local dev / scratch artifacts
.DS_Store
.playwright-mcp/
dashboard-live-view.png
escrow-test/
/run
# Go test TMPDIR (set via TMPDIR=.gotmp for -race) — never commit build/test cache
.gotmp/
