escrow.toml
escrow-cache/
/escrow
/escrow-cli
/escrow-darwin-*
/escrow-linux-*
/escrow-cli-darwin-*
/escrow-cli-linux-*
.superpowers/

# Go test TMPDIR (set via TMPDIR=.gotmp for -race) — never commit build/test cache
.gotmp/
